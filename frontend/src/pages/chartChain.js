import ChartPage from "@/components/charts/chartChain"

/**
 * create the chain chart page
 * @returns {JSX.Element}
 */
export default function BarChartChainPage() {
  return <ChartPage type="bar" title="Grafico a barre blockchain" />;
}
