import ChartPage from "@/components/charts/chartPageCheat"

/**
 * create the bar chart page cheated
 * @returns {JSX.Element}
 */
export default function BarChartCheatPage() {
  return <ChartPage type="bar" title="Grafico a barre truccato" />;
}
