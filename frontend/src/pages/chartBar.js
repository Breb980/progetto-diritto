import ChartPage from "@/components/charts/chartPage"

/**
 * create the bar chart page
 * @returns {JSX.Element}
 */
export default function BarChartPage() {
  return <ChartPage type="bar" title="Grafico a barre" />;
}
